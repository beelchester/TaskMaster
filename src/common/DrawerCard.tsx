import { ListItemButton } from "@mui/material";


interface DrawerCardProps {
  title: string;
  icon: string;
}

const DrawerCard = (props: DrawerCardProps) => {
  <ListItemButton>
    
  </ListItemButton>
}

export default DrawerCard